# 웹 서비스 (Render, 경량 Python)
flask>=3.0
gunicorn>=21.0
anthropic>=0.40          # Claude 비전 추출 (텍스트·수식·TikZ 생성)
pillow>=10.0             # 업로드 사진 다운스케일

# 참고: TikZ 렌더는 브라우저(TikZJax)에서 하므로 서버 LaTeX 불필요.
#      physlib(벡터 라이브러리)·테스트는 별도 개발용 — 배포에는 미포함.