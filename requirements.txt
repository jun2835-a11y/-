# ── 웹 서비스 (Render 배포) ────────────────────────────────────────
flask>=3.0
gunicorn>=21.0
anthropic>=0.40          # Claude 비전 추출
pillow>=10.0             # 업로드 사진 다운스케일
numpy>=1.24             # 그림 클린 스캔(배경 평탄화·대비)

# ── 렌더/라이브러리 ────────────────────────────────────────────────
# 순수 파이썬 휠, 시스템 의존성 없음 (physlib PNG 래스터화용; SVG만 쓰면 선택)
resvg-py>=0.3.0

# ── 테스트 (배포 시 불필요) ────────────────────────────────────────
pytest>=7.0